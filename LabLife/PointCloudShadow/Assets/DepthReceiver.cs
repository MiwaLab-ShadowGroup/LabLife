using UnityEngine;
using System.Collections;
public class DepthReceiver : MonoBehaviour {
    //public UDPDepthTest.DepthClient depthClient = new UDPDepthTest.DepthClient(512,424);
	// Use this for initialization
	void Start () {
        //this.depthClient.InitClients();
	}
	
	// Update is called once per frame
	void Update () {
        
        //Debug.Log( this.depthClient.Recieve().Length.ToString());
	}
}
